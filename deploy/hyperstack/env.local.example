# Copy to hello-agents-lab/.env on the Hyperstack machine (or merge keys).
# OpenAI-compatible vLLM on localhost — no real secret needed.

LLM_API_KEY=EMPTY
LLM_BASE_URL=http://127.0.0.1:8000/v1
# Must match vLLM --served-model-name
LLM_MODEL_ID=llama-3.3-70b-instruct

# Optional
# LLM_TIMEOUT=120
